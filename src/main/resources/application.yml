server:
 port: 9093
eureka:
 instance:
  preferIpAddress: true
  hostname: eureka-0
 client:
  service-url:
   defaultZone: http://localhost:8761/eureka/
spring:
 application:
  name: USER-SERVICE
 jpa:
  hibernate:
   ddl-auto: update
  show-sql: true
 